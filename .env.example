# Copy to .env (git-ignored) — or set these as real environment variables.

# Optional: enables the strictly opt-in cloud assist (also needs the Settings toggle).
# Free key at https://aistudio.google.com — leave unset to stay 100% local.
GEMINI_API_KEY=
